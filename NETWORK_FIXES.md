# תיקוני רשת - KungFu Chess

## מה תוקן:

### 1. תיקוני שרת (GameServer.cpp):
- ✅ הסרת קריאות לפונקציות שלא קיימות (`isValidCursorMove`, `isValidPieceSelection`, `isValidPieceMove`, `validateSelectCommand`)
- ✅ פישוט לוגיקת האימות ב-`validateInput`
- ✅ שיפור טיפול בשגיאות

### 2. תיקוני קליינט (GameClient.cpp):
- ✅ הוספת טיפול בשגיאות ב-`handleServerCommand`
- ✅ הוספת try-catch blocks למניעת קריסות
- ✅ הוספת include עבור `<stdexcept>`

### 3. סקריפטי הרצה:
- ✅ יצירת `build_and_run.ps1` - סקריפט מקיף לבנייה והרצה
- ✅ יצירת `quick_test.ps1` - בדיקה מהירה עם שרת ו-2 קליינטים

## איך להריץ:

### אופציה 1: סקריפט מקיף
```powershell
.\build_and_run.ps1
```
ואז בחר:
- 1 = שרת בלבד
- 2 = קליינט בלבד  
- 3 = שרת + קליינט
- 4 = משחק מקומי

### אופציה 2: בדיקה מהירה
```powershell
.\quick_test.ps1
```
מתחיל אוטומטית שרת + 2 קליינטים

### אופציה 3: ידני
```powershell
# בנייה
mkdir build
cd build
cmake .. -G "Visual Studio 16 2019" -A x64
cmake --build . --config Release
cd ..

# העתקת DLL
copy OpenCV_451\bin\opencv_world451.dll build\Release\

# הרצה
.\build\Release\KungFuChess_Server.exe    # טרמינל 1
.\build\Release\KungFuChess_Client.exe    # טרמינל 2
.\build\Release\KungFuChess_Client.exe    # טרמינל 3
```

## מה אמור לקרות:

1. **השרת מתחיל** ומחכה לחיבורים על פורט 8080
2. **קליינט ראשון מתחבר** ומקבל צבע WHITE (לבן)
3. **קליינט שני מתחבר** ומקבל צבע BLACK (שחור)
4. **המשחק מתחיל** - כל קליינט רואה את הלוח שלו
5. **שחקנים משחקים** עם חצים + Enter

## בקרות:
- **חצים**: תזוזת סמן
- **Enter**: בחירה/תזוזה
- **ESC**: יציאה

## מה עדיין צריך לבדוק:

### בעיות אפשריות:
1. **סנכרון**: האם שני הקליינטים רואים את אותו מצב?
2. **תזוזות**: האם תזוזות מועברות נכון בין קליינטים?
3. **גרפיקה**: האם החלונות נפתחים נכון?
4. **רשת**: האם החיבור יציב?

### דיבוג:
- הודעות דיבוג מופיעות בקונסול
- שרת מדפיס את כל הפעילות
- קליינטים מדפיסים הודעות קבלה ושליחה

## הבעיות שתוקנו:

### לפני התיקון:
❌ שרת קורס בגלל קריאות לפונקציות שלא קיימות
❌ קליינט קורס בגלל חוסר טיפול בשגיאות
❌ אין סקריפטי הרצה נוחים

### אחרי התיקון:
✅ שרת יציב עם אימות פשוט
✅ קליינט עם טיפול בשגיאות
✅ סקריפטים נוחים להרצה ובדיקה

## הצעדים הבאים:
1. הרץ `.\quick_test.ps1` לבדיקה מהירה
2. בדוק שהגרפיקה עובדת
3. בדוק שהתזוזות מסונכרנות
4. דווח על בעיות נוספות שמתגלות